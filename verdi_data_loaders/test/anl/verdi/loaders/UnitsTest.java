package anl.verdi.loaders;

import junit.framework.TestCase;

import java.io.IOException;

/**
 * @author Nick Collier
 * @version $Revision$ $Date$
 */
public class UnitsTest extends TestCase {

	public void testUnits() throws IOException {
		/*
		File f = new File("./plugins/pave2.data.loaders/test/anl/pave2/loaders/agg.cctmJ3fx.b312.nh3c1.dep_wa.annual.2001base");
		URL url = f.toURI().toURL();
		NetcdfFile file = new NetcdfFile(url);
    // NetcdfDataset dataset  = M3IOConvention.factory(file);
		List vars = dataset.getVariables();
		for (VariableDS var : (Iterable<VariableDS>)vars) {
			System.out.println("var = " + var);
		}
		*/
	}
}
